import React from 'react';
import Navbar from '../Navbar/Navbar';
import Footer from '../footer/Footer';

const Root = () => {
    return (
        <div>
            <Navbar></Navbar>
            <Footer></Footer>
        </div>
    );
};

export default Root;